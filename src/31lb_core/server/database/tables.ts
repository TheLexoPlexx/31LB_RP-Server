
const prefix = "31lbrp_";

let tables = {
  players: prefix + "players",
  places: prefix + "places",
  vehicles: prefix + "vehicles",
  weapons: prefix + "weapons",
  //weitere Tabellen hier einfügen
};

export default tables;